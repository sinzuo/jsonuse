index.c 编译为 index

param.c 编译为 main.cgi

mcu.c 编译为 mcu.cgi

add.c 编译为 add.cgi

delete.c 编译为 delete.cgi

upload.c 编译为 upload

--liyao 2012-12-18
新增http.cgi，原来的main.cgi和index暂时还保留
http.c json.c 编译为http.cgi

将编译好的文件放在cgi-bin目录下即可